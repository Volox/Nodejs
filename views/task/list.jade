extends ../layout

block content
	div.taskList
		h1 &micro;Task list for Task #{taskID} 
		a.addCode(href="/task/#{taskID}/code/add") Add code
		if data.microTasks
			ul
				each microTask in data.microTasks
					li
						a(href="/task/#{microTask.id}")= microTask.id