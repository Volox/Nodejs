// Generated by CoffeeScript 1.3.1
(function() {
  var ClassifyImage,
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor; child.__super__ = parent.prototype; return child; };

  ClassifyImage = (function(_super) {

    __extends(ClassifyImage, _super);

    ClassifyImage.name = 'ClassifyImage';

    function ClassifyImage() {
      this.run = __bind(this.run, this);

      this.createOuput = __bind(this.createOuput, this);

      this.getCategories = __bind(this.getCategories, this);

      this.getImages = __bind(this.getImages, this);

      this.createCategories = __bind(this.createCategories, this);

      this.init = __bind(this.init, this);
      return ClassifyImage.__super__.constructor.apply(this, arguments);
    }

    ClassifyImage.prototype.init = function() {
      this.categories = [];
      this.maxNum = 1;
      $('body').append($('<form>', {
        id: 'myForm'
      }));
      this.getConfiguration('category', this.getCategories);
    };

    ClassifyImage.prototype.createCategories = function() {
      var $element, $option, category, _i, _len, _ref;
      $element = $('<select>', {
        name: 'categories',
        size: this.maxNum === 1 ? 1 : 3,
        multiple: this.maxNum !== 1 ? true : false
      });
      _ref = this.categories;
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        category = _ref[_i];
        $option = $('<option>', {
          text: category,
          value: category
        });
        $element.append($option);
      }
      return $element;
    };

    ClassifyImage.prototype.getImages = function(error, json) {
      var $li, $ul, data, image, index, _i, _len;
      if (!error) {
        data = json.field['caption'];
        $('body').append($('<ul>', {
          id: 'imageList'
        }));
        $ul = $('#imageList');
        for (index = _i = 0, _len = data.length; _i < _len; index = ++_i) {
          image = data[index];
          $li = $('<li>');
          $li.attr('data-object-id', json.id[index]);
          $li.append($('<img>', {
            src: image,
            width: 100,
            height: 100,
            title: json.field['image'][index]
          }));
          $li.append(this.createCategories());
          $ul.append($li);
        }
      } else {
        alert('No data retrieved!');
      }
      this.$runBtn = $('<button>', {
        text: 'Send results',
        click: this.run
      });
      $('body').append(this.$runBtn);
    };

    ClassifyImage.prototype.getCategories = function(error, json) {
      if (!error) {
        this.categories = json;
        this.getData('*', this.getImages);
      } else {
        alert('No categories specified');
      }
    };

    ClassifyImage.prototype.createOuput = function() {
      var $ul, answers;
      this.storeData('id', parseInt(this.task));
      this.storeData('token', '');
      this.storeData('userId', '');
      answers = [];
      $ul = $('#imageList');
      $('li', $ul).each(function() {
        var $li, answerObj, categories;
        $li = $(this);
        answerObj = {};
        answerObj['objectId'] = parseInt($li.data('objectId'));
        categories = [];
        $('select option:selected', $li).each(function() {
          return categories.push($(this).val());
        });
        answerObj['categories'] = categories;
        answers.push(answerObj);
      });
      this.storeData('answers', answers);
    };

    ClassifyImage.prototype.run = function() {
      var _this = this;
      console.log('Run!');
      this.createOuput();
      this.postData(function(error, data) {
        if (!error) {
          _this.$runBtn.prop('disabled', true);
          return alert('Data sent successfully');
        } else {
          return alert("Unable to post results\n" + error);
        }
      });
    };

    return ClassifyImage;

  })(uTask);

  $(function() {
    var CI;
    return CI = new ClassifyImage;
  });

}).call(this);
