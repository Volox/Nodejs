// Generated by CoffeeScript 1.3.1
(function() {

  $(document).ready(function() {
    var $img, blur, blursteps, img, octave, octaves, _i, _j;
    $img = $('#img');
    octaves = 5;
    blursteps = 4;
    for (octave = _i = 1; 1 <= octaves ? _i <= octaves : _i >= octaves; octave = 1 <= octaves ? ++_i : --_i) {
      for (blur = _j = 1; 1 <= blursteps ? _j <= blursteps : _j >= blursteps; blur = 1 <= blursteps ? ++_j : --_j) {
        img = $('<img>', {
          id: "img_" + octave + "_" + blur,
          src: $img.prop('src'),
          width: $img.prop('width') / octave,
          height: $img.prop('height') / octave
        });
        $('.images').append(img);
        img.pixastic('blurfast', {
          amount: 0.25 * blur
        });
      }
    }
    /*
    	cx = $canvas.loadCanvas()
    
    	width = $canvas.width()
    	height = $canvas.height()
    	$canvas.drawImage
    		source: 'img/test.jpg'
    		fromCenter: false
    		width: width
    		height: height
    		load: ->
    			$canvas.setPixels
    				each: (px) ->
    					px.r = px.b
    					px.g = px.g
    					px.b = px.r
    */

  });

}).call(this);
